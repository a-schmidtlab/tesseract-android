<?xml version="1.0" encoding="utf-8"?>
<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.SofaRotator" parent="Theme.AppCompat.DayNight.NoActionBar">
        <!-- Material 3 color system -->
        <item name="android:statusBarColor">@color/background_black</item>
        <item name="android:navigationBarColor">@color/background_black</item>
        <item name="android:windowLightStatusBar">false</item>
    </style>

    <style name="Theme.SofaRotator" parent="Base.Theme.SofaRotator" />
</resources> 